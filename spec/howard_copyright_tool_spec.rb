require "spec_helper"

RSpec.describe HowardCopyrightTool do
  it "has a version number" do
    expect(HowardCopyrightTool::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
